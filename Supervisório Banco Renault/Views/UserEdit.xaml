<Window x:Class="Supervisório_Banco_Renault.Views.UserEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:enum="clr-namespace:Supervisório_Banco_Renault.Models.Enums"
             xmlns:local="clr-namespace:Supervisório_Banco_Renault.Views"
             mc:Ignorable="d" 
             Height="500" Width="400" Background="White" WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ObjectDataProvider x:Key="dataFromEnum" MethodName="GetValues" ObjectType="{x:Type enum:AccessLevel}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enum:AccessLevel"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        
        
        <StackPanel Margin="30">
            <Label Content="Nome:" FontSize="16"/>
            <TextBox Margin="5, 0" FontSize="16" Height="27.28" VerticalContentAlignment="Center" Text="{Binding User.Name}"/>
            <Label Content="Tipo de usuário:" FontSize="16"/>
            <ComboBox x:Name="userLevelComboBox" Margin="5, 0" FontSize="16" ItemsSource="{Binding Source={StaticResource dataFromEnum}}" SelectedItem="{Binding User.AccessLevel}"/>
            <TextBox Height="0" Width="0" Text="{Binding User.TagRFID}" x:Name="tagTextBox"/>
            <Button Content="Ler Crachá" FontSize="16" Margin="5,30,5,0" Click="OnClickButtonReadTag" Height="27.28"/>
            <Label Content="Senha:" FontSize="16"/>
            <PasswordBox x:Name="passwordBox" Margin="5, 0" FontSize="16" Height="27.28" VerticalContentAlignment="Center" PasswordChanged="OnPassChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Label Content="Cancelar" Width="130" FontSize="16" Style="{StaticResource HoverableLabelRedButton}" Cursor="Hand" Margin="10, 0" MouseDown="Cancel_MouseDown"/>
            <Label Content="Salvar usuário" Width="130" FontSize="16" Style="{StaticResource HoverableLabelGreenButton}" Cursor="Hand" Margin="10, 0" MouseDown="Add_MouseDown" />
        </StackPanel>

    </Grid>
    
</Window>
