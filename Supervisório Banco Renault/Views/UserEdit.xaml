<Window x:Class="Supervisório_Banco_Renault.Views.UserEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:enum="clr-namespace:Supervisório_Banco_Renault.Models.Enums"
             xmlns:local="clr-namespace:Supervisório_Banco_Renault.Views"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d" 
             Height="370" Width="420" WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="Transparent">

    <Border BorderBrush="Gray" BorderThickness="0.5" CornerRadius="10" Background="White" Effect="{StaticResource DropShadowEffect}" Width="400" Height="350">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>


            <StackPanel Margin="30,10" VerticalAlignment="Center">
                <TextBox FontSize="16" VerticalContentAlignment="Center" Text="{Binding User.Name}" ui:ControlHelper.Header="Nome:" Margin="0,0,0,10"/>
                <ComboBox x:Name="userLevelComboBox" FontSize="16" SelectedItem="{Binding User.AccessLevel}" HorizontalAlignment="Stretch" ui:ControlHelper.Header="Nivel de acesso:"/>
                <TextBox Height="0" Width="0" Text="{Binding User.TagRFID}" x:Name="tagTextBox" KeyDown="TagRFIDTextOnKeyDown" Style="{x:Null}"/>
                <Button x:Name="ReadRFIDButton" Content="Ler Crachá" FontSize="16" Margin="0,30,0,0" Click="OnClickButtonReadTag" Focusable="True" HorizontalAlignment="Stretch"/>
            </StackPanel>

            <Grid Grid.Row="1" HorizontalAlignment="Stretch" Margin="30,0">
                <Label Content="Salvar usuário" Width="150" FontSize="16" Style="{StaticResource HoverableLabelGreenButton}" Cursor="Hand" MouseDown="Add_MouseDown" HorizontalAlignment="Left"/>
                <Label Content="Cancelar" Width="150" FontSize="16" Style="{StaticResource HoverableLabelRedButton}" Cursor="Hand" MouseDown="Cancel_MouseDown" HorizontalAlignment="Right"/>
            </Grid>

        </Grid>
    </Border>

</Window>
